# Frontend Desktop Application Dependencies
# These are Python dependencies that might be needed for the desktop app

# Core dependencies
requests==2.32.3
websockets==12.0
python-dotenv==1.0.1

# Optional: For enhanced desktop features
pywin32>=306; sys_platform == "win32"
psutil==6.1.0
